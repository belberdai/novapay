info:
  name: health-check
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
