info:
  name: get-non-existent-account-aggregate
  type: http
  seq: 2

http:
  method: GET
  url: "{{host_analytics}}/analytics/accounts/ffffffff-ffff-ffff-ffff-ffffffffffff"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
