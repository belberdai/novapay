info:
  name: get-account-aggregate
  type: http
  seq: 2

http:
  method: GET
  url: "{{host_analytics}}/analytics/accounts/{{srcId}}"
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5